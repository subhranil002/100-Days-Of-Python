tup = (1, 5, 6) # Unchangeable
print(type(tup)) # <class 'tuple'>
print(tup) # (1, 5, 6)

# Indexings are same as Lists
# Method uses are same as Lists
# The only difference between lists and tuple is tuples are immutable and lists are mutable